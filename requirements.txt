# Quantum Under The Hood — pinned environment
# Python 3.13. Install with:  pip install -r requirements.txt
#
# The [visualization] extra is REQUIRED, not optional: it supplies pylatexenc,
# without which QuantumCircuit.draw('mpl') raises MissingOptionalLibraryError.
qiskit[visualization]==2.5.1
qiskit-aer==0.17.2

numpy==2.3.3
scipy==1.16.3
matplotlib==3.10.6

# Notebook authoring / execution toolchain
nbformat==5.10.4
nbconvert==7.16.6
ipykernel==6.30.1
jupyter==1.1.1

# NOTE: qiskit-algorithms is deliberately NOT a dependency. It was spun out of the
# SDK in Qiskit 1.0 and is no longer supported by IBM. VQE and QAOA in this repo are
# hand-rolled on EstimatorV2 + scipy.optimize.minimize, which is better teaching
# material anyway. See CLAUDE.md.
